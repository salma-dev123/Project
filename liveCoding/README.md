# Prototype Convertisseur CLI
ce projet est un prototype php qui convertit des données à partir de fichier JSON en utilisant une interface CLI

# installation 
1. cloner le depot
2. installer les dépendances avec 'composer install'