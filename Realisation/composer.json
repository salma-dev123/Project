{
  "name": "salma/mini-calculator",
  "description": "Convertisseur et mini-calculatrice CLI (décimal ↔ binaire/hex, opérations logiques, batch)",
  "type": "project",
  "require": {
    "php": ">=8.2"
  },
  "autoload": {
    "psr-4": {
      "App\\": "src/"
    }
  },
  "scripts": {
  "start": "php bin/calc.php",
  "build": "composer lint && composer test",
  "validate-project": "composer validate --strict",
  "lint": "php -l src/*.php",
  "test": "php bin/convert.php --help",
  "format": "echo \"Formatage du code (placeholder)\"",
  "save": "php bin/calc.php > samples/output.json"
}

}
